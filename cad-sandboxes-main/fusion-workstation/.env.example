# Use the normal AWS CLI credential chain (profile, SSO, environment, or role).
# Optional: point to an ignored file containing AWS_* settings.
FUSION_AWS_ENV_FILE=""
# Optional external modeling CLI installation. The CLI tools themselves are separate.
FUSION_MODELING_ROOT=""
